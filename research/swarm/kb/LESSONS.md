# LESSONS — the desk's own process failures (append-only, dated)

- 2026-09-16 — v1 discarded its own screens because phases handed off prose summaries. Rule: artifacts (`out.json`) are the only handoff.
- 2026-09-16 — v1 asserted "needs 56.5% WR" for two unrelated ideas with no derivation. Rule: all WR/time numbers come from `fee_math`.
- 2026-09-16 — v1 ideation was indicator-named; 8/8 were relabels. Rule: thesis schema requires counterparty + nearest dead rows.
- 2026-09-16 — v1 ran two workflows concurrently and died 4× on a shared rate ceiling. Rule: one workflow at a time.
- 2026-09-16 — v1 never explored multi-day / event-driven horizons though in scope. Rule: the desk brief must allocate at least two analyst lenses to horizons > 8h.
- 2026-09-16 — dryrun-2026-09-16: the analyst seat was spawned with its WebSearch budget already exhausted (0 searches possible) and ran anyway; the run's only thesis (research/swarm/runs/dryrun-2026-09-16/theses/dryrun_forced_flows_delever_fade.json) carried no externally sourced number, failing STANDARDS #2 independently of its gate rejection (research/swarm/runs/dryrun-2026-09-16/CRITIC.md section d.2). Rule: the orchestrator checks the search budget at analyst spawn and aborts the analyst stage (never silently continues) when it reads exhausted; a thesis with zero fetched numbers is a plumbing condition, not a research finding.
- 2026-09-16 — dryrun-2026-09-16: REPORT.md line 15 quoted the analyst's signal sanity-check numbers (20304 bars per symbol, values {-1,0,1}, 196/179/253 nonzero) from stdout that was never persisted; the critic had to re-run the signal on era=train to create research/swarm/runs/dryrun-2026-09-16/critic/signal_sanity_train.txt (CRITIC.md section b.1). Rule: every analyst sanity-check output (bar counts, value set, nonzero count per symbol) is written to a file under theses/ before any report cites it.
- 2026-09-16 — dryrun-2026-09-16: REPORT.md line 20 stated "budget exhausted at 200/200", a figure that appears in no file in the run dir (CRITIC.md section b.2). Rule: a number with no file path is dropped from the report or explicitly attributed to the orchestrator's run context; STANDARDS #11 applies to plumbing numbers too.
- 2026-09-16 — dryrun-2026-09-16: the sole thesis died at the gate as a deliberate relabel of DEAD_LIST row 6 (research/swarm/runs/dryrun-2026-09-16/gate_rejections.json), so registrar freeze, screen, audit, the tp +/-20% robustness read and committee were never exercised; the plumbing test covered only lens -> gate -> critic -> reconcile (CRITIC.md "What was not done"). Rule: a plumbing dry run includes one thesis the gate is expected to KEEP (a non-relabel with a real fetched number) so every downstream stage produces its artifacts and surfaces stage-level errors before a real run.
- 2026-09-16 — dryrun-2026-09-16: at critic time no STANDARDS #15 reconcile line existed for the run and REPORT.md line 22 deferred it to the orchestrator (CRITIC.md "Other process notes" 1); closed by this reconcile seat — nothing was screened, so these LESSONS lines are the run's only kb write (no DEAD_LIST or SURVIVORS row). Rule: the reconcile seat runs after the critic in every run, dry runs included, and writes at least one dated LESSONS line even when zero theses were screened.
- 2026-09-17 — dryrun-2026-09-17-0230: the critic found no process failures (no holdout access, no sha drift, no hand-rolled numbers, no ROI target, no orphaned artifacts — research/swarm/runs/dryrun-2026-09-17-0230/CRITIC.md "Summary"); nothing was screened (research/swarm/runs/dryrun-2026-09-17-0230/gate_kept.json is []; no screens/ dir, no out.json), so these LESSONS lines are the run's only kb write — no DEAD_LIST or SURVIVORS row. Rule: STANDARDS #15 rows are written only for theses with screens/<id>/out.json; gate-rejected theses stay in REPORT.md and gate_rejections.json.
- 2026-09-17 — dryrun-2026-09-17-0230: REPORT.md lines 33-34 quoted lens claims ("web work capped at 1 WebSearch + 1 WebFetch", "signal validated to compile and emit only {-1, 0, 1} on a synthetic frame", web_budget_exhausted: false) from the orchestrator run context with no file in the run dir (research/swarm/runs/dryrun-2026-09-17-0230/CRITIC.md section b.1-b.2) — the same defect as the 2026-09-16 "200/200" line, recurring. Rule: the orchestrator writes every lens's exploratory_notes, web-call counts and web_budget_exhausted flag to a file in the run dir (lenses.json) before the report stage runs; a lens claim with no path is dropped from the report.
- 2026-09-17 — dryrun-2026-09-17-0230: the sole thesis (research/swarm/runs/dryrun-2026-09-17-0230/theses/dryrun_forced_flows_deleverage_snapback.json lines 36-39: timeframe 5m, max_hold_bars 48 = 4h) breached mandate.md section 4 (no 5-minute-horizon mechanisms; two lenses on holds > 8h), yet gate_rejections.json and REPORT.md rejected it on dead row 6 alone and never surfaced the mandate breach; launch_args.json max_analysts 1 also made the mandate's two->8h-lens requirement unsatisfiable and the report did not say so (CRITIC.md section c.1-c.2). Rule: the gatekeeper checks every thesis against the mandate's horizon and lens-allocation clauses and records each breach in gate_rejections.json alongside the dead-row reason; the mandate seat must not require more lenses than launch_args.json max_analysts allows, or must flag the shortfall in the mandate itself.
- 2026-09-17 — dryrun-2026-09-17-0230: REPORT.md lines 16-20 cited lib outputs (p_star, time_to_verdict_weeks, position_notional, lot_check, C_BPS, list_symbols) to library source paths rather than to any output artifact in the run dir; the critic had to re-run them to verify (CRITIC.md section b.3). Rule: seats that reproduce lib numbers for a report save the command output to a file under the run dir and cite that path, not the library source file (STANDARDS #11).
- 2026-09-19 — run 2026-09-19-1451: `exploratory/cross_asset/probe_ethbtc_regime.py` (output `probe_ethbtc_regime.out.txt`) generated the h=48 mean_fwd_bps numbers that evidently shaped the registered `cross_asset_ethbtc_regime_rotation` spec's `max_hold_bars=48` and universe, but neither file was cited in the thesis's `evidence` field or in REPORT.md (CRITIC.md section (a).1). Rule: every exploratory probe whose output shapes a thesis's registered spec parameters must be cited by path in that thesis's `evidence` field.
- 2026-09-19 — run 2026-09-19-1451: REPORT.md's `forced_flows` and `session_calendar` lens paragraphs stated search/fetch counts and dead-row mappings with zero backing file — no `exploratory/forced_flows/` or `exploratory/session_calendar/` directory exists anywhere in the run tree (CRITIC.md sections (b).1-2). Rule: every lens's exploratory notes and claim counts must be persisted to a file under `exploratory/<lens>/` before the report cites them (recurs from the 2026-09-16/2026-09-17 dryrun lessons on unbacked lens claims — still not fixed).
- 2026-09-19 — run 2026-09-19-1451: REPORT.md's opening, verdict-setting sentence miscounted theses as "8 registered... 6 screened" when the artifacts show 6 registered (`theses/*.json` minus the self-declared probe), 5 screened (`screens/*/out.json`), 1 gate-rejected (CRITIC.md section (c).1). Rule: the report-writing seat must programmatically count registered/screened/gate-rejected theses from the run-dir artifact directories, never state a remembered count.
- 2026-09-19 — run 2026-09-19-1451: REPORT.md computed "0.14pp" (WR minus p*, stated twice) and "~200+ weeks to n=50" by hand subtraction/arithmetic in prose instead of via `research.swarm.lib` (CRITIC.md sections (b).2 and (g).2-3). Rule: STANDARDS #8's hand-arithmetic ban covers every derived figure in report prose, including simple deltas between two already-cited numbers, not only primary CI/WR/p*/time-to-verdict outputs.
- 2026-09-19 — run 2026-09-19-1451: `theses/vol_structure_mm_withdrawal_drift.json` states the thesis is sized at a "72-bar" horizon while its own frozen spec (`specs/vol_structure_mm_withdrawal_drift.frozen.json:thesis.spec.max_hold_bars`) and the screen it was run at both use 48 bars (CRITIC.md section (c).2). Rule: the registrar freeze step must diff prose horizon/parameter claims in `theses/*.json` against the frozen spec's own numeric fields and refuse to freeze on a mismatch.
- 2026-09-19 — run 2026-09-19-1451: `exploratory/literature/local_check_copy.py` read raw CSVs directly from an undocumented directory (`backtest_data_june`) via `pd.read_csv`, bypassing `research.swarm.lib.load_data` and therefore its holdout-boundary enforcement entirely (CRITIC.md section (e)). Rule: any exploratory probe that reads price/time-series data must go through `load_data`, or the probe's ad hoc source and date range must be explicitly justified against the era boundary in its own output.
- 2026-09-20 — run 2026-09-20-0300: REPORT.md never cited by path any of the 11 `exploratory/` probe files, any of the 4 completed screens' `trades.csv`, or any of the 5 `specs/*.frozen.json`, even though several exploratory probes are the actual evidentiary basis for claims the report repeats via `gate_kept.json`/thesis-file paraphrase (CRITIC.md section a). Rule: the report-writing seat must cite the exploratory probe(s), `trades.csv`, and frozen spec path underlying each claim it makes, not only `out.json`/`audit.json`.
- 2026-09-20 — run 2026-09-20-0300: REPORT.md attributed 5 claims — including a substantive sourcing-verification discrepancy alleged against vol_structure_volscale_continuation's cited papers — to an unfilable "run context" with no corresponding file anywhere in the run directory (CRITIC.md section b). Rule: recurs from the 2026-09-16/2026-09-17 dry-run lessons on unbacked run-context/lens claims (still not fixed) — the orchestrator must persist run-context state (screened/committee_passed arrays, audit_verdict annotations, lens sourcing-verification notes) to a named file in the run dir before the report stage cites it, or the report must drop the claim.
- 2026-09-19 — build 2026-09-19-1451/informed_flow_btc_alt_cascade: holdout read void (n=0) because the frozen signal loaded BTC with era="train" hard-coded; screen now supplies the era via load_data.screen_context and signals must use load_reference; the thesis was re-registered byte-identically as informed_flow_btc_alt_cascade_v2 (era plumbing only) — process failure, not a strategy result.
- 2026-09-21 — paper informed_flow_btc_alt_cascade_v2 killed on the $-10 any-n cap after ONE event. The slot holds one $200 position per alt across 16 alts (up to $3,200 notional) while the dollar cap was sized to the $200 capital basis; one BTC pump fired 5 shorts over two consecutive bars (4 open at once) that stopped together (-$16.20 in 3h). The screen scored trades as independent and the prereg set no concurrency or per-event cap, so the kill line measured event clustering, not per-trade edge. The verdict stands (pre-registered). Fix for future preregs: cap concurrent positions per event, or size per event = capital basis / expected cluster, and state it in the prereg.
- 2026-09-21 — GIGGLE/USDT:USDT is DELISTED on Phemex (ccxt market active=False, last 1h bar 2026-08-07 10:00 UTC) but the long_1h cache ends 2026-08-01, so the screen and prereg carried an untradeable symbol (the slot re-fetched it every cycle, stamp stuck at Aug 7). lot_check verifies lot size only, not market status. Fix: screen/build must check market['active'] for every universe symbol before prereg and drop delisted ones with a LESSONS line.
- 2026-09-21 — process change after the informed_flow_btc_alt_cascade_v2 kill (line above): the chain spec → screen → prereg → slot had no concurrency cap anywhere. Fixed in code: `fee_math.max_concurrent(sl_bps)` (= 3 at sl 150 bps, $200 notional, $10 cap); `registrar.freeze` fills `spec.max_concurrent` before hashing (legacy frozen files without the key still verify and screen byte-identically); `screen.run_screen` admits trades in (entry_ts, universe order) under the cap and reports `out.json.portfolio`; `build.js` PREREG refuses a spec without the key (SPEC_MISSING_MAX_CONCURRENT), states the cap + derivation in Frozen data / Economics, adds it to the anti-fishing list, and the IMPLEMENT recipe requires `MAX_CONCURRENT` with an entry-skip guard + test; STANDARDS #17. Rule: no thesis reaches a paper slot without a frozen concurrency cap the dollar kill line was sized to.
- 2026-09-25 — maint: 5m_mean_revert KILLED 2026-09-24 (bot kill switch: negative Kelly (-0.001) after 57 trades; n=57, net $-0.10). Rule: maint writes the DEAD_LIST/SURVIVORS rows the same day; the desk reconciler never duplicates them; maint promotes, kills and restarts nothing.
- 2026-09-25 — run 2026-09-25-2036: REPORT.md was never written (synthesis seat returned no file; CRITIC.md sections a, b, g), so every run artifact is uncited and the five zero-thesis lenses (informed_flow, session_calendar, dealer_inventory, vol_structure, literature) have no "What was not done" record. Rule: desk.js closeOut must assert REPORT.md exists and is non-empty after the synthesis seat and stop with an explicit error before the critic/reconciler run, instead of continuing on a null synthesis.
- 2026-09-25 — run 2026-09-25-2036: the thesis owner_record_delist_peg_dislocation_fade and gate_rejections.json state "33 trades in 46.68 weeks = 0.707/week" and time_to_verdict 70.73 with no file path; 0.707/week is hand arithmetic and fee_math.time_to_verdict_weeks(0.707) returns 70.72135785007073 (CRITIC.md b.2, c.5). Rule: trade frequency must be computed by a probe script that writes it to a cited output file, and time_to_verdict must be the printed fee_math output from that exact input (STANDARDS #8).
- 2026-09-25 — run 2026-09-25-2036: the cross_asset_brrny_window_continuation thesis evidence and gate_kept.json claim every universe symbol passes lot_check at $200, but out.json lot_check shows lot_usd null for 13 of 18 symbols (not in fee_math.LOT_MIN_USD), so their lot minimums are unverified, not passed (CRITIC.md c.1; audit.json findings). Rule: a lot_check result with lot_usd null counts as UNVERIFIED; the thesis and the gatekeeper must say so explicitly, and a thesis may not claim CONSTRAINTS viable item 4 until LOT_MIN_USD covers every universe symbol.
- 2026-09-25 — run 2026-09-25-2036: cross_asset_brrny_window_continuation evidence cited placebo hours 13/14/17 as reverting, but left out the NY hour-10 placebo, which also continued (exploratory/cross_asset/probe_fix_hour.out.txt:16-24), and the contrary probe_session_split.out.txt (US->post corr negative on 9 of 10 symbols); the gatekeeper kept it anyway (CRITIC.md c.2, c.3). Rule: thesis evidence must report every placebo/control block and every contrary probe output the lens produced, and the gatekeeper must check the cited probe file in full, not only the lines quoted.
- 2026-09-25 — run 2026-09-25-2036: mandate.md section 4 requires at least two analyst lenses to target holds > 8h, and neither thesis did (screened spec max_hold 6 x 1h; gate-rejected spec 12 x 5m) (CRITIC.md c.7). Rule: the gatekeeper must check mandate horizon quotas before closing the gate and record any shortfall as a named mandate miss in gate_kept.json.
- 2026-09-25 — run 2026-09-25-2036: exploratory/vol_structure/probe_letf_close_fade.py and probe_lv_upside_break.py ran screen.simulate + admit_trades + bootstrap_ci.mean_ci on train across unregistered direction/universe/tp/k variants (4 and 2 variants), and probe_letf_close_fade_specparams.out.txt has no matching script or recorded command (CRITIC.md a.4, a.5). Rule: every exploratory output file must have a committed script plus the exact command line that produced it, and probes that call screen.simulate/bootstrap on train must list every variant tried in a notes file so the search is visible to multiplicity review.
- 2026-09-25 — run 2026-09-25-2036: REPEAT of LESSONS 2026-09-19 — exploratory/literature/local_check_copy.py again read raw CSVs from backtest_data_june via pd.read_csv, bypassing load_data; the data (BTC 2026-05-20 -> 2026-07-04, per local_check_output.txt:2) lies inside the long_1h holdout window. No thesis used it (CRITIC.md e.2). Rule: the 2026-09-19 rule is not being followed by prompt alone, so the analyst prompt must forbid pd.read_csv/read_parquet/read_pickle on price data outside research.swarm.lib.load_data, and the critic grep must flag any such call as a process failure.
