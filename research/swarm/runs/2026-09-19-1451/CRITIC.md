# CRITIC — run 2026-09-19-1451

Method: read `REPORT.md`, listed the run dir recursively, then re-opened every artifact cited (or that should have been cited) — `gate_kept.json`, `gate_rejections.json`, all 5 `screens/<id>/out.json` + `audit.json`, `committee/{bh,economics,statistics}.json`, `theses/*.json`, `specs/*.frozen.json`, `exploratory/**`, `out.robust_*.json` — and cross-checked every number in the report against the file that should back it.

Full file list (`find research/swarm/runs/2026-09-19-1451 -type f | sort`): 63 files (excluding `__pycache__/*.pyc`, which are not artifacts).

---

## (a) Artifacts not cited in the report

1. **`exploratory/cross_asset/probe_ethbtc_regime.py` and `probe_ethbtc_regime.out.txt`** — orphaned. Neither file is referenced anywhere in `REPORT.md`, nor in `theses/cross_asset_ethbtc_regime_rotation.json`'s `evidence` field (which cites only the two external URLs, dobermanvc.com and coingecko.com). Yet the probe output (`probe_ethbtc_regime.out.txt:1-13`) is exactly the kind of exploratory shaping evidence that would explain the registered spec's `max_hold_bars=48` and universe choice (it tests h=12/24/48 on SOL/ADA/XRP/LINK and finds `mean_fwd_bps` at h=48 of 22.26/9.82/23.02/21.6, vs negative at h=12/24) — the probe was run but its result was never linked to the thesis it evidently informed, or to the report. Per STANDARDS #11 ("Artifacts, not prose... Orphaned artifacts are a critic finding") this is a defect: a real evidentiary number was generated and then dropped instead of cited.
2. **`exploratory/informed_flow/exploratory_trades.csv`** — the trades file backing `exploratory/informed_flow/exploratory_out.json` is never cited directly (only the summary `exploratory_out.json` and `probe_btc_alt_lag.py` are referenced in `theses/informed_flow_btc_alt_cascade.json`). Minor — the summary it feeds is cited, so this is a lesser case than (1).
3. **`specs/*.frozen.json` (all 5)** — never cited by path in `REPORT.md` itself (though `committee/economics.json` does cite `informed_flow_btc_alt_cascade.frozen.json` for `max_hold_bars`/`timeframe`). Since STANDARDS #9 explicitly permits committee seats to read the frozen spec, this isn't a violation, but the report's own prose (e.g. "16 universe symbols", "max_hold=7 bars×1h=7h") is sourced from the committee file rather than linking the spec directly — traceable but one hop removed.
4. **`screens/informed_flow_btc_alt_cascade/robust/{make_variants.py, thesis_minus.json, thesis_plus.json, minus/specs/*.frozen.json, minus/screens/.../{out.json,signal.py,trades.csv}, plus/specs/*.frozen.json, plus/screens/.../{out.json,signal.py,trades.csv}}`** — none of these ~10 files are cited in the report; it cites only the top-level copies `out.robust_plus.json` / `out.robust_minus.json`. Verified these top-level files are byte-identical to the nested `robust/plus/screens/.../out.json` and `robust/minus/screens/.../out.json` (`diff` shows no output), so this is duplication rather than a hidden discrepancy, but the nested tree (registrar-frozen sub-specs, signal copies, trades.csv) is otherwise unlinked from any report text.

## (b) Numbers in the report without a file path

1. **Entire `forced_flows` lens paragraph** (REPORT.md line ~35): "4 searches + 1 fetch (curupira.dev)... rows 6/80... row 29... row 76... rows 4/7/20/34/82/83/88... row 10" — zero file path for any of these counts or row-mapping claims. There is no `exploratory/forced_flows/` directory anywhere in the run tree (confirmed by the recursive file listing above) and no thesis file for this lens, so none of these numbers/claims link to an artifact at all.
2. **Entire `session_calendar` lens paragraph** (REPORT.md line ~36): "~6 searches + 2 fetches (Quantpedia, and the full Nazaruk 2025 FOMC/CPI primary-source PDF)... all CAR p>0.27... ~0.23-0.25 trades/week implies ~200+ weeks to n=50" — again, no `exploratory/session_calendar/` directory exists in the run tree. The "~200+ weeks to n=50" figure is also a hand-computed time-to-verdict number in prose rather than a `fee_math.time_to_verdict_weeks()` output cited by path, which independently trips the "never hand-roll... time-to-verdict" rule (see (g) below).
3. Both of the above are process failures under STANDARDS #11 ("A candidate without `out.json` does not exist... every number in a report links to a file") — these two lenses' entire contribution to the report is unbacked prose.

## (c) Contradictions between gate_rejections.json / theses/*.json / out.json / audit.json / committee/*.json / out.robust_*.json and the report

1. **Report's opening tally is wrong.** REPORT.md line 3: "8 theses were registered this run... 6 were screened (1 was gate-rejected as a duplicate before screening)." Actual artifact counts: `theses/*.json` contains exactly 6 real thesis files (`cross_asset_btc_shock_lag`, `cross_asset_ethbtc_regime_rotation`, `dealer_inventory_gamma_flush`, `informed_flow_btc_alt_cascade`, `vol_structure_gamma_unclamp`, `vol_structure_mm_withdrawal_drift` — the 7th file, `theses/owner_record_probe.json`, is explicitly self-labeled `"note": "PROBE, not a thesis or a screen result"`). `gate_kept.json` has 5 entries and `gate_rejections.json` has 1 entry (5+1=6, matching the 6 real thesis files — internally consistent with each other). `screens/` contains exactly 5 subdirectories with `out.json` (matching `gate_kept.json`'s 5). So the true counts are **6 registered, 5 screened, 1 gate-rejected** — the report's "8 registered" and "6 screened" are both off (by 2 and 1 respectively), stated as the report's very first, verdict-setting sentence.
2. **`vol_structure_mm_withdrawal_drift` hold-length figure conflicts with its own frozen spec.** `theses/vol_structure_mm_withdrawal_drift.json`'s `nearest_dead_rows[0].why_different` states the thesis is "sized at a multi-day (**72-bar**) horizon", but `specs/vol_structure_mm_withdrawal_drift.frozen.json:thesis.spec.max_hold_bars` = **48**, and `screens/vol_structure_mm_withdrawal_drift/out.json` was screened at that same 48-bar registered spec. The report (line 15) doesn't repeat the "72-bar" figure, so the report itself isn't wrong here, but the thesis document — the pre-registration artifact — contradicts the frozen spec it supposedly describes.
3. No contradictions found between `out.json` ↔ `audit.json` ↔ `committee/*.json` ↔ report for the numeric results themselves: `n`, `net_bps_mean`, `ci95`, `wr`, `p_star`, `p_boot`, `trades_per_week`, `time_to_verdict_weeks` for all 5 screened theses were independently re-read from `out.json`/`audit.json` and match the report's figures exactly (cross_asset_btc_shock_lag: n=1518, net_bps_mean=-15.578, ci95=[-22.174,-8.381], wr=0.4690, p_star=0.53833; cross_asset_ethbtc_regime_rotation: n=729, net_bps_mean=-31.011, ci95=[-45.119,-16.701], wr=0.4513; vol_structure_gamma_unclamp: n=3, net_bps_mean=-44.833, ci95=[-161.5,188.5], wr=0.3333, time_to_verdict_weeks=714.087; vol_structure_mm_withdrawal_drift: n=1, net_bps_mean=-161.5, ci95=null; informed_flow_btc_alt_cascade: n=246, net_bps_mean=34.071, ci95=[12.271,56.301], wr=0.5244, p_star=0.523, p_boot=0.0025). `committee/bh.json` ranks/thresholds match the report line-by-line. `out.robust_plus.json` (n=236, net_bps_mean=38.989, ci95=[13.662,62.952]) and `out.robust_minus.json` (n=252, net_bps_mean=22.246, ci95=[2.190,41.144]) match the report's robustness paragraph exactly, and independently verified byte-identical to the nested `robust/plus/` and `robust/minus/` copies. Month-spread (68/246=27.64%) and per-symbol dominance (NEAR/TAO tied at 25/246=10.16%) were independently recomputed from `trades.csv` and match `committee/statistics.json` and the report.
4. `gate_rejections.json`'s single entry (`dealer_inventory_gamma_flush`) has `"dead_row": null` even though STANDARDS #13 requires citing the dead list by row — acceptable here only because the rejection reason is dedup-against-a-sibling-thesis rather than a dead-list relabel, but the field is technically empty where the schema implies a row number; the report's description of this rejection (line ~9) is otherwise a faithful paraphrase of `gate_rejections.json`'s `reason` text.

## (d) Thesis with empty nearest_dead_rows / generic why_different / no number in evidence

None. All 6 filed theses (`cross_asset_btc_shock_lag`, `cross_asset_ethbtc_regime_rotation`, `dealer_inventory_gamma_flush`, `informed_flow_btc_alt_cascade`, `vol_structure_gamma_unclamp`, `vol_structure_mm_withdrawal_drift`) have non-empty `nearest_dead_rows` (3-4 rows each, all verified to exist and match their gist in `research/swarm/kb/DEAD_LIST.md`), each with a specific, mechanism-based `why_different` (no generic "different timeframe" boilerplate — each names a structural distinction: no basket/no short leg, exogenous-trigger-not-own-asset, continuation-not-reversion, etc.), and each `evidence` field contains at least one concrete number from a named source (e.g. dealer_inventory_gamma_flush's own exploratory probe: "76% of weeks for BTC (mean 282.5bps post vs 205.7bps pre)"; vol_structure_gamma_unclamp: "24-27% short vs 42-46% long" realized vol). `theses/owner_record_probe.json` has no `nearest_dead_rows` field at all, but it is explicitly self-declared not a thesis (`"note": "PROBE, not a thesis or a screen result"`), so this schema field's absence there is not a defect.

## (e) Holdout access

`grep -rn` for `COMMITTEE-HOLDOUT-READ|era="holdout"|era='holdout'|era="all"|--era holdout|--era all` across the run dir returns exactly **one hit**: `mandate.md:62` — `"Holdout = final 25% of each dataset's date range; it is **never read in this run** — no \`era="holdout"\`/\`"all"\`..."`. This is the mandate *stating the prohibition*, not an actual holdout read (confirmed: every `out.json`'s `era` field inspected above reads `"train"`; no `out.holdout.json`/`trades.holdout.csv` files exist anywhere in the tree, and STANDARDS #7's era-suffix rule for non-train artifacts was never triggered). So this is a benign grep match, not a process failure.

Related but not literally matching the grep pattern: `exploratory/literature/local_check_copy.py` reads raw CSVs directly from `/Users/jonaspenaso/Desktop/Phmex-S/backtest_data_june` (`local_check_copy.py:2-3`, `SRC="/Users/jonaspenaso/Desktop/Phmex-S/backtest_data_june"`), bypassing `research.swarm.lib.load_data` entirely. This directory is **not listed** in `research/swarm/kb/DATA.md` under either canonical dataset (`mr_edge`, `long_1h`) or the "other bot-collected exploratory-only data" list (`l2_ticks`, `flow_capture`, `entry_snapshots`). Because it's read via plain `pd.read_csv` rather than `load_data.load_ohlcv`, it receives none of `load_data`'s holdout-boundary enforcement — there is no way to confirm from the artifacts alone whether the 2026-05-20→07-04 window this script reads (`local_check_output.txt:1`) falls entirely inside any dataset's train era. This doesn't match the literal grep (no `era=` string appears — the script has no era parameter at all), but it is the same underlying risk the holdout-gating rule exists to prevent, via an unlisted, ungated data path. Flagging as a process-integrity concern rather than a confirmed holdout breach, since DATA.md doesn't define an era boundary for this undocumented directory at all.

## (f) Frozen spec / signal.py sha verification

```
$ python3 -c "from research.swarm.lib import registrar as r; import glob; print({p: r.verify(p) for p in glob.glob('research/swarm/runs/2026-09-19-1451/specs/*.frozen.json')})"
{'.../specs/cross_asset_btc_shock_lag.frozen.json': True,
 '.../specs/vol_structure_mm_withdrawal_drift.frozen.json': True,
 '.../specs/vol_structure_gamma_unclamp.frozen.json': True,
 '.../specs/cross_asset_ethbtc_regime_rotation.frozen.json': True,
 '.../specs/informed_flow_btc_alt_cascade.frozen.json': True}
```
All 5 frozen specs verify. None. (Note: `dealer_inventory_gamma_flush` has no frozen spec file at all — it was gate-rejected before the freeze step, which is consistent with STANDARDS #3's pre-register-then-read ordering only if rejection is treated as pre-registration; there is nothing to verify for it either way.)

## (g) Daily-ROI target or hand-computed statistic in the report

1. **No daily-ROI target found anywhere** in `REPORT.md` (`grep -in "roi\|daily.*target\|per day"` returns zero hits) — the mandate's owner-aspiration figure (+10%/day, `mandate.md` §1) is correctly kept out of the report entirely, as instructed.
2. **Hand-computed statistic:** REPORT.md lines 25 and 51 both state "WR beats p* by only 0.14pp" / "WR 52.44% vs p* 52.3%, a 0.14pp edge". This is prose subtraction (0.524390... − 0.523 = 0.00139... ≈ 0.14pp) done directly in the report text rather than via a `research.swarm.lib` call, and it is not sourced to any file the way every other number in this report is (contrast with the surrounding sentence, which cites `out.json` for both operands but not for the delta itself). It is arithmetically correct and low-stakes (a subtraction of two already-cited, lib-derived numbers, not a new p-value/CI/win-rate/time-to-verdict computation), but it is technically hand arithmetic in prose per STANDARDS #8, appearing twice.
3. The "~200+ weeks to n=50" figure in the `session_calendar` paragraph (line ~36) is both hand-computed (not via `fee_math.time_to_verdict_weeks`) and unsourced to any file — already flagged under (b) above; noted here again because it doubles as a hand-rolled time-to-verdict statistic, which STANDARDS explicitly lists as a lib-only computation.
