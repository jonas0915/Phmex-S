# Synthesis Report — run 2026-09-19-1451

**Verdict first:** 8 theses were registered this run; 6 were screened (1 was gate-rejected as a duplicate before screening); of those 6, 1 passed committee — `informed_flow_btc_alt_cascade` — and 5 failed on the numbers despite clean, reproducible screen mechanics.

---

## Thesis-by-thesis

### Gate-rejected (never screened)

**`dealer_inventory_gamma_flush`** — one line, per `research/swarm/runs/2026-09-19-1451/gate_rejections.json`: rejected as a **duplicate** of `vol_structure_gamma_unclamp`. Both name the identical forced counterparty (Deribit BTC/ETH options dealers unwinding a gamma-driven delta hedge at expiry) and trade the identical instrument/direction (Phemex BTC/ETH perp breakout continuation post-expiry); the only differences (weekly vs monthly/quarterly cadence, 6h vs 72h hold) are parameters, not mechanism. `vol_structure_gamma_unclamp` was kept as the better-specified sibling (it gates on a pre-expiry realized-vol compression precondition; this one fired on every weekly expiry unconditionally and cited large-expiry evidence for a small-expiry population).

### Screened, failed committee

**`vol_structure_mm_withdrawal_drift`** (evidence grade A) — n=1, net_bps_mean=-161.50, ci95=null (bootstrap needs n≥2), WR=0.0% vs p*(200)=52.875% — all from `research/swarm/runs/2026-09-19-1451/screens/vol_structure_mm_withdrawal_drift/out.json`. Audit verdict CONFIRMED (process integrity only — reproducible rerun, causality PASS) per `.../audit.json`, but findings there flag n=1 as "far below CONSTRAINTS viability item 1's n≥30 minimum" and the single trade (XRP short, 2025-10-11) hit stop-loss intrabar. p_boot=1.0 (`.../audit.json:numbers.p_boot`). BH: rank 2/5, threshold 0.04, not significant (`research/swarm/runs/2026-09-19-1451/committee/bh.json`). No economics/statistics committee vote was cast for this thesis (only `informed_flow_btc_alt_cascade` appears in `committee/economics.json` and `committee/statistics.json` vote lists) — it never reached that stage because n=1 fails viability item 1 outright. No tp±20% robustness read was run (no `out.robust_*.json` files exist for this id) — not applicable given the fatal n=1.

**`vol_structure_gamma_unclamp`** (evidence grade A) — n=3, net_bps_mean=-44.83, ci95=[-161.50, 188.50], WR=33.33% vs p*(200)=52.875%, trades_per_week=0.070, time_to_verdict_weeks=714.09 — all from `research/swarm/runs/2026-09-19-1451/screens/vol_structure_gamma_unclamp/out.json`. Audit CONFIRMED (process integrity) per `.../audit.json`; findings flag n=3 well below 30, CI straddling zero, WR below p*, and time-to-verdict (714 weeks) far past the 26-week bar. p_boot=1.0 (`.../audit.json:numbers.p_boot`). BH: rank 3/5, threshold 0.06, not significant (`committee/bh.json`). No economics/statistics vote recorded for this id — same story, killed on n before committee. No robustness read run (no `out.robust_*.json`).

**`cross_asset_btc_shock_lag`** (evidence grade B) — n=1518, net_bps_mean=-15.58, ci95=[-22.17, -8.38], WR=46.90% vs p*(150)=53.83% — `research/swarm/runs/2026-09-19-1451/screens/cross_asset_btc_shock_lag/out.json`. Audit CONFIRMED per `.../audit.json`; its own finding states the CI excludes zero but "lies entirely in negative territory (opposite sign of the thesis's predicted positive drift)" — the mechanism is refuted by train-era data, not merely inconclusive. p_boot=1.0 (`.../audit.json:numbers.p_boot`). BH: rank 4/5, threshold 0.08, not significant (`committee/bh.json`). No economics/statistics vote recorded. No robustness read run.

**`cross_asset_ethbtc_regime_rotation`** (evidence grade B) — n=729, net_bps_mean=-31.01, ci95=[-45.12, -16.70], WR=45.13% vs p*(200)=52.875% — `research/swarm/runs/2026-09-19-1451/screens/cross_asset_ethbtc_regime_rotation/out.json`. Audit CONFIRMED per `.../audit.json`: "Thesis fails viability on the numbers themselves... CI is entirely negative." p_boot=1.0 (`.../audit.json:numbers.p_boot`). BH: rank 5/5, threshold 0.10, not significant (`committee/bh.json`). No economics/statistics vote recorded. No robustness read run.

### Screened, passed committee

**`informed_flow_btc_alt_cascade`** (evidence grade A) — n=246, net_bps_mean=+34.07, ci95=[12.27, 56.30] (excludes zero on the positive side), WR=52.44% vs p*(250)=52.3% — `research/swarm/runs/2026-09-19-1451/screens/informed_flow_btc_alt_cascade/out.json`. WR beats p* by only 0.14pp — a real but narrow in-sample margin. Audit verdict CONFIRMED, `ci_excludes_zero: true`, zero findings flagged — `.../audit.json`. p_boot=0.0025 (`.../audit.json:numbers.p_boot`).
- **Economics committee: pass**, per `research/swarm/runs/2026-09-19-1451/committee/economics.json` — WR≥p* confirmed, lot_check ok for all 16 universe symbols at $200 notional, feasible on the 5-min poller (1h timeframe, closed-bar signal, causality PASS), max_hold=7 bars×1h=7h ≤ 8h so no funding adjustment required, time_to_verdict_weeks=10.17 ≤ 26.
- **Statistics committee: pass**, per `research/swarm/runs/2026-09-19-1451/committee/statistics.json` — n=246≥30; CI95 both bounds >0; BH at q=0.10/m=5 gives this thesis rank 1, p=0.0025 ≤ threshold (1/5)×0.10=0.02, k*=1, only this id significant (full table `committee/bh.json`); month-spread from trades.csv shows max single month 2026-02 at 68/246=27.6% (< 50% concentration cap); per-symbol dominance max is TAO/NEAR at 25/246=10.2% (< 60% cap, universe=16).
- **Robustness (tp_bps ±20% of registered 250 bps, STANDARDS #9):** registered net_bps_mean=+34.07 (`out.json`); +20% variant (tp=300) net_bps_mean=+38.99, n=236, ci95=[13.66, 62.95] (`out.robust_plus.json`); −20% variant (tp=200) net_bps_mean=+22.25, n=252, ci95=[2.19, 41.14] (`out.robust_minus.json`). Same sign (positive) in both variants — pass, per `committee/statistics.json` item (f).

---

## What was not done

**Analyst lenses that returned zero theses** (each with its own reasoning, from the run context's `lenses` field — not re-derived here):
- **forced_flows** — 4 searches + 1 fetch (curupira.dev). Every mechanism traced back to an existing dead row (liquidation-cascade fade → rows 6/80; continuation → row 29; multi-day overshoot-after-cascade → collapses into row 76's failure mode once the real minutes-scale timing of the forced-flow window is accounted for; funding-settlement flow → rows 4/7/20/34/82/83/88; OI/ADL proxy → row 10, blocked by data availability). Stopped voluntarily, not budget-exhausted.
- **session_calendar** — ~6 searches + 2 fetches (Quantpedia, and the full Nazaruk 2025 FOMC/CPI primary-source PDF). FOMC/CPI thesis killed because the actual paper (not the search-summary, which stated a number not in the paper) shows only significant abnormal *volatility*, not returns (all CAR p>0.27) — a magnitude-not-direction finding, the same failure mode as dead row 82. CME futures-expiry thesis killed on frequency: ~0.23-0.25 trades/week implies ~200+ weeks to n=50, far past the 26-week cap, and the source gave no bps magnitude to size against. Also found but discarded: a weekend crypto-leads-TradFi-Monday-open paper that measures the wrong market (TradFi cash open, not the asset's own Phemex price) — using it would require inventing a missing number.
- **literature** — 5 pre-fetched papers + 8 searches + 4 fetches + a read-only local probe (`research/swarm/runs/2026-09-19-1451/exploratory/literature/local_check_copy.py`, output in `local_check_output.txt`). Every mechanism was either sub-fee by the source's own numbers (SSRN 6932998 states costs consume 93% of its own risk allocation; the probe's BTC-leads-alts signed drift pooled to +1.71bps gross vs 11.5bps `fee_math.C_BPS`, corroborated by arXiv 2608.21888's ~1.3bp edge against a 5bp cost), a relabel of an existing dead family (calendar → row 7/67; volume-profile → row 25; liquidation continuation → rows 6/80), or uncomputable by this bot's infra (Twitter/GPT sentiment, sub-minute HFT microstructure, DEX/Uniswap feeds).
- **owner-record** — full run of `research/swarm/runs/2026-09-19-1451/exploratory/owner-record/probe.py` against the 819-row `api_closed_pnl.json`; probe output at `research/swarm/runs/2026-09-19-1451/theses/owner_record_probe.json`. Found the account's entire lifetime realized profit (+$4,857.22 cumulative) is driven by one day (2022-11-10, u100TRYBUSD, +$8,128.01); excluding that day the remaining 785 trades net -$3,288.65 with a losing 0.65 payoff ratio. No thesis: the profitable event is n=1, uses a legacy inverse contract not in either screenable dataset, needed sub-minute precision, and both candidate mechanisms (reversion or continuation after the event) are already dead rows (6/28/80/84).

**Lenses with `web_budget_exhausted: true`:** none. Every lens in this run's context (`forced_flows`, `informed_flow`, `dealer_inventory`, `session_calendar`, `vol_structure`, `cross_asset`, `literature`, `owner-record`) reports `web_budget_exhausted: false` — no lens had its sourcing cut short by a tool refusal; all stops were voluntary once the mechanism space was exhausted.

**Registrar/screen/audit errors:** none recorded. All 6 screened-and-attempted specs (5 gate-kept theses screened + robustness variants for `informed_flow_btc_alt_cascade`) show `causality: "PASS"` and audit verdict `CONFIRMED` with no `screen_error` field populated, per the run context's `screened` list and each `out.json`/`audit.json`. `vol_structure_mm_withdrawal_drift` and `vol_structure_gamma_unclamp` did not get a robustness read or a full economics/statistics committee vote, but that is because they failed viability item 1 (n≥30) outright, not because of a tool error.

**Sources that could not be fetched:** two WebFetch attempts in the `vol_structure` lens (ScienceDirect and SSRN pages on Bitcoin-options-expiry gamma studies) returned HTTP 403 and were explicitly excluded from citation, per that lens's own notes — only WebSearch snippet titles surfaced, which the lens correctly treated as insufficient to cite. In `cross_asset`, two further fetches (a Springer paywalled article and a raw PDF for a DergiPark paper) failed to return readable text; the lens used search-snippet content only for those two and flagged it explicitly rather than inventing page detail. In `owner-record`, no equity curve in USD could be built because the deposit/withdrawal files are in raw per-currency units, not USD — cumulative realized PnL was reported instead, with the gap noted in the probe's `equity_path_caveat` field.

---

## Next run should

- Investigate why the two `vol_structure` theses (both evidence grade A, well-sourced) produced only n=1 and n=3 trades over the full `long_1h` train span — before drafting another options-expiry or MM-withdrawal thesis, check whether the trigger condition in `signal.py` is simply too narrow (e.g. an AND of multiple rare preconditions) versus the mechanism being real but low-frequency; a trigger this rare cannot reach n≥30 inside any reasonable dataset, so either loosen the precondition (new thesis) or accept the mechanism is structurally unscreenable at this desk's dataset size.
- For the one thesis that passed committee (`informed_flow_btc_alt_cascade`), note the margin is thin (WR 52.44% vs p* 52.3%, a 0.14pp edge) — the next run's build/paper-slot stage should treat this as a "deploy small, watch closely" candidate rather than a confirmed edge, and should re-verify the robustness reads' CI lower bounds (both variants' CI95 lower bounds are positive but the minus-variant's is only 2.19 bps) don't erode further under the real ~27% maker-fill rate per CONSTRAINTS.md fill-realism note.
- Multiple lenses this run independently rediscovered the same dead-row families (calendar/funding effects, liquidation-cascade reversion/continuation, volume-profile/S-R bounce) from different entry angles (owner's trade history, academic literature, forced-flows search) — consolidate this into a single sourced "why these families stay dead" note in the KB so future lenses can skip re-deriving it from scratch each run and spend more search budget on genuinely unexplored mechanism space.
