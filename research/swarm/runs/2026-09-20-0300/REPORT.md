# Swarm Report — run 2026-09-20-0300

## Verdict first

9 theses were proposed across 8 analyst lenses; 5 were gate-kept and 4 were gate-rejected pre-screen; of the 5 gate-kept, 4 completed a screen run (1 errored on a syntax error in its own frozen signal file before producing any result); all 4 completed screens came back audit-verdict CONFIRMED on process integrity but **zero cleared CONSTRAINTS.md's viability bar** (every one had a negative or zero-straddling net-bps CI, and every observed win rate was below its p*); **zero theses reached or passed committee this run** (`gate_kept.json` n=5, `gate_rejections.json` n=4, run-context `screened`/`committee_passed` arrays).

## Screened theses

**forced_flows_adl_winner_reversal** (evidence grade A, `gate_kept.json`) — mechanism: ADL force-closes the most-profitable opposite-side positions at the extreme tail of a cascade, injecting forced flow against the move (source: bluefin.io, cube.exchange, datawallet.com, arxiv 2603.15963, per thesis file). Screen: n=65, net_bps_mean=-10.92, ci95=[-33.34, 10.45] (`screens/forced_flows_adl_winner_reversal/out.json`), wr=46.15% vs p*=53.83% (`out.json`) — CI straddles zero and WR is below p*, so both viability items 1 and 2 fail. Audit verdict CONFIRMED, `ci_excludes_zero`=false, no findings flagged beyond the numbers themselves (`screens/forced_flows_adl_winner_reversal/audit.json`). Committee: not run — no `committee/*.json` artifact exists anywhere under this run's `screens/` tree and `committee_passed` is empty in the run context, so there is no BH table or tp±20% robustness read to cite for this thesis.

**forced_flows_daily_relever_flow** (evidence grade B, `gate_kept.json`) — mechanism: leveraged-token issuers (BTC3L/3S, ETH3L/3S style products) must rebalance daily near UTC close to restore fixed leverage, creating forced same-direction flow (source: kucoin.com/learn; two other cited sources — support.amun.com and ascendex.com — could not be verified, see "not done" below). Screen: n=477, net_bps_mean=-34.61, ci95=[-45.40, -23.18] (`screens/forced_flows_daily_relever_flow/out.json`) — fully negative, statistically distinguishable from zero but on the wrong side; wr=38.36% vs p*=53.83% (`out.json`). Audit verdict CONFIRMED (process-integrity sense only); its findings explicitly state the thesis "fails CONSTRAINTS.md 'viable' item 1/2" and that the observed WR "triggers" the spec's own pre-registered kill condition, i.e. the audit recommends this be treated as refuted-by-data (`screens/forced_flows_daily_relever_flow/audit.json`). Committee: not run, same as above.

**informed_flow_btc_alt_lag** (evidence grade A, `gate_kept.json`, ranked 3/7 pre-screen) — mechanism: BTC's price move is public and re-priced fastest in the deepest book; slower-liquidity alt books catch up with a lag, which this thesis trades as continuation (source: Springer 10.1007/s10690-026-09589-z, arxiv 2506.08718). This screen never produced a result: `screens/informed_flow_btc_alt_lag/signal.py` line 1 is a malformed docstring that literally opens with the file's own path/description text ("screens/informed_flow_btc_alt_lag/signal.py (see file; reproduced): \"\"\"informed_flow_btc_alt_lag …") instead of valid Python, which I confirmed by reading the file directly — `screen.run_screen`'s `load_signal_fn` raises `SyntaxError: invalid syntax … Perhaps you forgot a comma?` on import. There is no `out.json`, `audit.json`, or `trades.csv` for this id on disk. The run context records `audit_verdict: "REFUTED"` for it, but that label has no corresponding `audit.json` file to cite — it is a run-context annotation only, not a file-backed audit verdict, and that discrepancy should be flagged rather than repeated as fact.

**owner_record_liq_cascade_continuation** (evidence grade A, `gate_kept.json`) — mechanism: a 1h bar with |z|>2.5 return plus volume z>2.0 proxies a forced-deleveraging event; the forced flow extends the move before exhausting (sources: arxiv 2608.03616, coinchange.io Nov-2025 cascade writeup, both confirmed verbatim per `gate_kept.json`; locally informed by, not evidenced by, `research/swarm/kb/owner_trades/api_closed_pnl.json` via `theses/owner_record_probe.json`). Screen: n=136, net_bps_mean=-23.89, ci95=[-43.72, -3.85] (`screens/owner_record_liq_cascade_continuation/out.json`) — fully negative CI; wr=35.29% vs p*=52.875% (`out.json`). Audit verdict CONFIRMED on process integrity (frozen sha verified, rerun byte-identical, causality clean, 10/10 spot-checked trade rows matched, p*/time-to-verdict recomputed exactly); the audit's own findings state the thesis's pre-registered DOA line ("kill if … CI includes zero, OR WR < p*(200)") is triggered on both conditions and recommends it be recorded as a killed/DEAD_LIST candidate citing dead rows 6 and 80 (`screens/owner_record_liq_cascade_continuation/audit.json`). Committee: not run.

**vol_structure_volscale_continuation** (evidence grade A, `gate_kept.json`) — mechanism: crypto vol-targeting/CTA funds scale UP (avg 1.14x) after a realized-vol regime shift, unlike equity vol-targeters (0.9x, de-risking), forcing mean-reversion traders on the wrong side to cover (sources: Springer 10.1007/s11408-025-00474-9, ScienceDirect S1544612325011377, Zenodo 19671502). Screen: n=762, net_bps_mean=-3.81, ci95=[-19.61, 12.21] (`screens/vol_structure_volscale_continuation/out.json`) — straddles zero; wr=43.57% vs p*=52.3% (`out.json`). Audit verdict CONFIRMED; findings state net_bps_mean is negative and the CI "does not exclude 0," and WR is below p*(250), while noting process integrity (byte-identical rerun, no lookahead, causality PASS) is otherwise intact (`screens/vol_structure_volscale_continuation/audit.json`). Committee: not run. Note a sourcing discrepancy: `gate_kept.json`'s reason for this id states "all 3 numeric claims confirmed independently … via WebFetch/WebSearch," while the vol_structure lens's own run-context notes say both WebFetch attempts on the primary Springer/ScienceDirect papers were blocked (403/303) and that the cited numbers "come from the WebSearch result summaries of those pages, not a directly-fetched full page" — these two accounts disagree on how the source numbers were actually verified and neither this session nor the run artifacts resolve which is correct.

## Gate-rejected theses (from `gate_rejections.json`)

- **literature_btc_alt_drift** — dead_row: none; reason: duplicate of informed_flow_btc_alt_lag (same BTC-leads-alt mechanism; informed_flow_btc_alt_lag had an a-priori literature threshold vs. this thesis's train-data-derived threshold, and confirmed sources vs. this thesis's unconfirmed primary source).
- **session_calendar_deribit_gamma_fade** — dead_row: none; reason: source verification F — its own cited source (menthorq.com) says short-gamma dealers trade WITH the trend (amplify into expiry, reverse after), the opposite mechanism/window from the pre-expiry dampening fade the spec and signal actually traded.
- **cross_asset_shock_fade** — dead_row: none; reason: over cap — ranked 6 of 7 survivors, sources confirmed (grade B), but cut for rank.
- **session_calendar_cme_expiry_drift** — dead_row: none; reason: over cap — ranked 7 of 7 survivors, sources confirmed verbatim (grade A), but its own pre-registered expected_trades_per_week=0.46 self-flags a high risk of failing the 26-week time-to-verdict bar, and it was cut for rank.

## What was not done

- **dealer_inventory lens returned 0 theses.** Its notes: every genuine forced-flattening mechanism it could source (Deribit options-dealer gamma hedging, funding/basis reversion, weekend/session-close seasonality, liquidation-cascade dealer unwind, ETF AP creation/redemption) either duplicates an already-screened/killed family this account (options-gamma: DEAD_LIST row 105 / `research/swarm/runs/2026-09-19-1451/screens/vol_structure_gamma_unclamp/out.json`; funding: owner-banned per STANDARDS #14; weekend seasonality: DEAD_LIST rows 7/18; liquidation-cascade reversion: DEAD_LIST rows 6/80) or is not buildable from data `research.swarm.lib.load_data` exposes (ETF flow has no loadable series). No thesis was forced to fill the slot.
- **web_budget_exhausted** is false for every lens in this run's context — no lens's sourcing was cut short by budget.
- **Registrar/screen error:** `informed_flow_btc_alt_lag`'s frozen signal file fails to import — `SyntaxError: invalid syntax` at `screens/informed_flow_btc_alt_lag/signal.py` line 1 (malformed docstring literally containing the file's own path as its first line), confirmed by direct read of the file this turn. No `out.json`/`audit.json` exist for it.
- **Committee stage never ran this run.** `committee_passed` is empty in the run context and no `committee/*.json` (or any `*robust*`) file exists anywhere under `research/swarm/runs/2026-09-20-0300/screens/` (confirmed by directory search). So for all 4 completed screens: no BH table, no economics/statistics committee votes, and no registered tp±20% robustness read (`out.robust_*.json`) exist to cite — "not run" for all of it.
- **Sources that could not be fetched:** `support.amun.com` (DNS-dead) and `ascendex.com` (403, archive.org unreachable) for forced_flows_daily_relever_flow (`gate_kept.json`); `sciencedirect.com/.../S1544612326008688` (403) cited by session_calendar_deribit_gamma_fade's second source and separately hit 403 by the dealer_inventory lens; ScienceDirect and ResearchGate abstract pages for Guo/Sang/Tu/Wang (403) and an empty SMU library page, both in the literature lens; the literature lens's `academic/ssrn_6932998.html` returned "Content Blocked" and was not cited; the cross_asset lens hit 403 on ScienceDirect/ResearchGate/SSRN/Tandfonline and did not cite them; the informed_flow lens had one Springer article paywalled/redirected and one dergipark PDF return unreadable binary, neither cited.

## Next run should

- Lint/syntax-check a signal file before `registrar.freeze` commits it — `informed_flow_btc_alt_lag` was the desk's best-sourced surviving thesis this lens produced (evidence grade A, ranked 3/7 pre-screen) and it burned a gate slot without ever producing a screenable result because of a trivially catchable malformed docstring.
- Run the committee stage (economics/statistics seats, BH table, the registered tp±20% robustness read) on every completed screen, including ones the audit already shows failing — this run produced 4 mechanically CONFIRMED audits and zero committee artifacts, so STANDARDS #9's process was not exercised at all this run.
- Reconcile outstanding orphaned/unreconciled artifacts before the next run: the forced_flows lens flagged `research/swarm/runs/2026-09-17-2020/theses/forced_flows_liq_cascade_continuation.json` as a prior, unscreened thesis that is in neither DEAD_LIST.md nor SURVIVORS.md; and per STANDARDS #15 this run's 4 audited-but-failing theses (all negative/zero-straddling net bps, all WR below p*) still need to be written into DEAD_LIST.md, which did not happen as part of this run's artifacts.
