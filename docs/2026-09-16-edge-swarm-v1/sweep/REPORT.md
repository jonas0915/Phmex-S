# Edge Sweep Report — 2026-09-16

## 1. Verdict

Nothing to build. All 4 ideas pulled from the internet sweep failed — 1 was dropped for fabricated/unverifiable evidence before vetting even started, and the other 3 were refuted during vetting. Zero candidates are going to paper.

The pattern across all 3 vetted candidates is the same: after realistic taker-taker fees (0.12% round trip) plus the required 4.5bps adverse-drift haircut, each one needs either a win rate or a trade frequency this account cannot plausibly deliver — and two of the three are also just relabeled versions of levers already killed on this account's own data (funding-level-predicts-price, taker entries, time-of-day gates).

No paper slot specs below — there's nothing that survived to spec out.

## 2. Paper slot specs (PASS / INCONCLUSIVE candidates)

None. All 3 vetted candidates (S1, S2, S3) were refuted (survives: false). No slot spec is drafted this round.

I did not open 02_framework_audit.md for this reason — that file matters for scoping what the slot framework would need to build, and with nothing passing, there's nothing to scope. It's available for the next sweep once a candidate survives vetting.

## 3. FAIL / refuted at vetting — one line each

- **S1 — Funding-settlement volatility-regime breakout gate (alts), grade A, refuted 2/2:** Relabels two already-dead levers (time-of-day gate, taker entries — both killed on this account's own data, 01_dead_list.md rows #7/#49/#67), and the fee+drift math needs a 56.5% win rate from a mechanism (funding-window vol clustering) that's a magnitude finding, not a direction finding — could just as easily mean more whipsaw as more continuation.
- **S2 — Funding-rate z-score contrarian reversal, grade C, refuted 2/2:** The bot's own 195-day funding study already measured this exact relationship at R²≈0.003 (no signal) and an owner directive says don't re-run the funding/XS/OI hunt; separately, at ~0.4-0.6 trades/week vs. ~150bps of 8h BTC volatility, a 95% CI verdict would take ~7-20 years.
- **S3 — FOMC/CPI first-candle momentum continuation (ETH), grade B, refuted 1/2 (net FAIL):** Genuinely novel mechanism (not a relabel of anything on the dead list), but the cost-adjusted breakeven win rate (56.5%) is asserted with zero pulled data behind it, confirmed frequency (~20/yr) would take 10+ years to reach a CI verdict, and the entry rule needs 1-minute precision at an exact release timestamp — the bot's 5-minute poller can't execute that as specified.

## 4. Killed at source verification (fabricated/unsupported evidence)

- **Social-sentiment-relative-to-own-trend daily directional bias (FNG proxy for Twitter polarization):** The strong backtest numbers (Sharpe 1.77, 0.32% mean daily return) belong to a proprietary 2011-2014 Twitter dataset that's not reproducible today; the candidate then swapped in a completely different, freely-available signal (Crypto Fear & Greed Index) with zero backtest evidence of its own — real paper, numbers that don't back the proposed live signal.

## 5. Honest limits

- This was a paper-sweep-of-ideas exercise, not a live data pull. None of the three vetted candidates had actual price/funding/OHLCV data fetched and computed in this task — win-rate and cost breakeven numbers are arithmetic on stated fee/haircut assumptions (03_exchange_economics.md), not measured backtest results. Where a vote says a mechanism was already measured on this account (S1's time-of-day/taker findings, S2's funding R²), that's a citation to the existing record, not new verification done here.
- S3's "no data pulled" gap is explicitly flagged by one voter (medium confidence, not-refuted) — it's the one candidate where the refutation rests more on frequency/execution-architecture math than on a disproven mechanism, so if the owner ever wants FOMC/CPI momentum re-examined, it'd need an actual timestamped-release + 1-min-bar pull first, not another internet sweep.
- Only 4 raw ideas came out of this sweep and only 3 survived dedup — a thin batch. Nothing here should be read as "the space is exhausted," only as "these 3 specific mechanisms don't clear this account's cost/frequency bar."
- I did not independently re-derive the fee/drift constants (0.12% RT taker-taker, 4.5bps haircut) — those are taken as given per the task's hard rules and cited from 03_exchange_economics.md by the vetting votes; I did not re-open that file to re-verify the cents-level ledger reconciliation myself in this task.

## Critic: gaps

- **4 full-text papers sit at sweep/ root, unnamed and undispositioned anywhere in the report:** `emoji_paper.txt` (Zuo, Chen & Härdle, "Emoji Driven Crypto Assets Market Reactions"), `garcia_schweitzer.txt` (Garcia & Schweitzer, Royal Society Open Science 2:150288, 2015), `hansen_periodicity.txt` (Hansen, Kim & Kimbrough, arXiv 2109.12142, "Periodicity in Cryptocurrency Volatility and Liquidity"), `petukhina_hft.txt` (Petukhina & Reule, arXiv 2009.04200, "Rise of the Machines?"). None appears as an idea, a FAIL, or an Honest-Limits line. Next step: open each, pull the concrete tradeable mechanism + number, and either vet it as a candidate or log an explicit reason for discarding it.
- **Section 4's killed idea gives numbers with no URL, even though the source file on disk has the citation.** I checked `garcia_schweitzer.txt:618` — Sharpe 1.7653 / mean daily return 0.3229% for the "Combined" strategy are accurate to the paper (not fabricated), but the report never prints `http://rsos.royalsocietypublishing.org/content/2/9/150288`. Next step: add the DOI/URL to section 4 so the claim is checkable without re-opening the raw file.
- **S3's "zero pulled data" claim (section 5, line 30) is contradicted by `event/kse.txt` + `event/kse_btc_cpi_fomc.pdf`** — a full 2025 Kyiv School of Economics thesis, Nazaruk, "Bitcoin's Reaction to U.S. FOMC and CPI Announcements," fetched and sitting in the event/ folder, directly on S3's topic. It was never opened for the vetting. Next step: read its event-window return/vol tables, re-run S3's cost-breakeven arithmetic against real numbers, or correct the honest-limits line to say the data existed but was unused.
- **`leadlag/local_check.py` is a real, runnable script (BTC→alt lead-lag, funding-stamp-minute clustering, quarter-hour bar effects) against the bot's own `backtest_data_june` 5m CSVs, but no output/log file exists anywhere in scratchpad/ or sweep/.** Can't tell if it ran. Next step: `python3 leadlag/local_check.py` and fold the printed tables into whichever candidate (S1?) it was meant to support, or delete it if superseded.
- **opensource/ modality effectively returned nothing but isn't flagged as null.** `sn_ranking.php.html` (517B) is confirmed (by reading it) to be strat.ninja's JS-redirect stub — `window.location.replace(...)`, no ranking data; `sn_dry.php.html` is 11 bytes; `sn_overview.php_strategy_NostalgiaForInfinityX6.html` is 484 bytes. Next step: re-fetch strat.ninja with a renderer or its API, or state explicitly in the report that this source came back empty.
- **opensource/ also holds a live Hyperliquid "Passivbot Canon" vault pull** (`vault_state.json`, `vault_fills.json`, and `passivbot_vault.json` re-fetched fresh at the sweep root 02:44, ~5h after the first 21:28 copy) — `vault_state.json` shows real current unrealized losses (FIL −$341.91, INJ −$42.57) on a well-known open-source grid/DCA bot. Never cited in the report. Next step: read `vault_fills.json` for realized win rate/avg-trade and log a FAIL (with citation) or an explicit reason grid/DCA is dead-list-adjacent and out of scope.
- **onchain/ modality (4 fetched papers — `onchain_flows_2411.06327`, `presto_whale_alerts`, `stablecoin_var_drpress`, `tether_mint_2501.05232`) contributes zero to the "4 raw ideas" count and isn't mentioned as screened-and-null vs. unprocessed.** Next step: state explicitly whether onchain was vetted and came back dry, and if unprocessed, pull one concrete signal (e.g. a whale-alert size threshold, a stablecoin-mint lead time) and run it through the same S1-S3 vetting.
- **`academic/ssrn_6932998.html` (896KB, fully fetched) is never opened or cited**, even though `academic/openalex_abstracts.txt` flags its own abstract as "NO ABSTRACT" for a 2026 paper unusually close to this account's actual instrument set ("Volume Profile Mean Reversion Strategy with Tape Speed Confirmation... A 5-Year Backtest Study on SOL/USDT Perpetual Contracts"). Next step: read the SSRN HTML directly for its real backtest numbers and cost assumptions before writing it off by omission.
- **Section 5's blanket claim ("None of the three vetted candidates had actual price/funding/OHLCV data fetched") overstates how empty-handed the sweep was** given the FOMC/CPI thesis (gap #3 above) and the runnable lead-lag script against real local OHLCV (gap #4). Next step: once #3/#4 are resolved, rewrite section 5 so it doesn't claim zero data was on hand when some was — just unused.
- **"Only 4 raw ideas... only 3 survived dedup" (line 31) has no backing artifact** — no idea-log file listing all pre-dedup candidate titles exists in sweep/, so the count can't be audited independently of the summary's own assertion. Next step: keep a raw-idea log inside sweep/ (one line per candidate considered, pre-dedup) on the next sweep so counts are checkable.
- **The "refuted 2/2" / "refuted 1/2" vote tallies for S1-S3 have no per-voter artifact in sweep/** — no vote transcript, scorecard, or reasoning file for either voter on any of the 3 candidates, only the raw source dumps (event/, leadlag/, academic/) that presumably fed them. Next step: save each voter's verdict + reasoning to its own file (e.g. `sweep/verdicts/S1_vote1.md`) so "refuted N/M" is traceable to actual text.
- **Fill-model question is moot this round, confirmed by reading section 2 (not assumed):** all three vetted candidates are marked `survives: false`, zero PASS/INCONCLUSIVE, so no PASS exists to have rested on an unrealistic fill model. Flag for the next sweep: once a candidate does pass, verify its fill assumption matches the task's hard rule (taker-taker entry+exit, fill at next bar/tick after signal) before it goes to paper.
